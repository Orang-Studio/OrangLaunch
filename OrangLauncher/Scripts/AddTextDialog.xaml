<Window x:Class="OrangLauncher.AddTextDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input Dialog"
        Height="160" Width="350"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Background="{DynamicResource BgPrimaryBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Grid.Row="0" Text="Enter text:" Margin="0,0,0,10" 
                   Foreground="{DynamicResource FgPrimaryBrush}"/>
        <TextBox x:Name="InputTextBox" Grid.Row="1" Height="28" 
                 Background="{DynamicResource BgSecondaryBrush}" 
                 Foreground="{DynamicResource FgPrimaryBrush}"
                 BorderBrush="{DynamicResource BorderPrimaryBrush}"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="OkButton" Content="OK" Width="75" Margin="0,0,10,0" Click="OkButton_Click"
                    Background="{DynamicResource AccentPrimaryBrush}" Foreground="{DynamicResource FgPrimaryBrush}"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="75" Click="CancelButton_Click"
                    Background="{DynamicResource BgSecondaryBrush}" Foreground="{DynamicResource FgPrimaryBrush}"/>
        </StackPanel>
    </Grid>
</Window>
