<Window
    x:Class="OrangLauncher.DownloadWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OrangLauncher"
    Title="Download Mods">
    <Grid Background="{ThemeResource BgPrimaryBrush}" Padding="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchTextBox" Grid.Column="0" Height="36" Padding="12,6"
                     FontSize="14" KeyDown="SearchTextBox_KeyDown"/>
            <Button Grid.Column="1" Content="Search" Width="100" Height="36" Margin="10,0,0,0"
                    Click="SearchButton_Click"/>
        </Grid>
        <ListView x:Name="ResultsListBox" Grid.Row="1" SelectionMode="None"
                  Background="{ThemeResource BgSecondaryBrush}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:SearchResult">
                    <Grid Padding="8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" FontSize="14"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind Description}" FontSize="12" Opacity="0.7"
                                       TextTrimming="CharacterEllipsis" MaxLines="2" TextWrapping="Wrap"/>
                            <TextBlock Text="{x:Bind Author}" FontSize="11" Opacity="0.5" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="Install" VerticalAlignment="Center"
                                Padding="16,6" Click="InstallProjectButton_Click" Tag="{x:Bind}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ProgressBar x:Name="SearchProgressBar" Grid.Row="2" Height="4" Margin="0,10,0,0"
                     IsIndeterminate="False" Visibility="Collapsed"/>
    </Grid>
</Window>
